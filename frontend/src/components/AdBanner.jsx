import React from 'react';

const AdBanner = ({ slot }) => {
  // Returns null to hide testing/dummy ads.
  // When you are ready to monetize with Google AdSense, paste your script here
  // and map the corresponding slot IDs (header, sidebar, processing, etc.).
  return null;
};

export default AdBanner;
